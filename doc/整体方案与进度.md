# 浏览器邮箱插件 — 整体方案与当前进度

> 本文档是项目**面向当下的整体方案与进度快照**，与《技术选型文档.md》（历史演进与详细选型）配套阅读。
> 更新日期：2026-09-04 ｜ 当前代码版本：**v1.0.0**（首个商店发布版本）

---

## 1. 产品与需求

| 维度 | 内容 |
|------|------|
| 形态 | Microsoft Edge / Chrome 浏览器扩展（**MV3**） |
| 目标邮箱 | 163 / QQ / USTC / Gmail |
| 核心功能 | 未读检查、桌面通知、快速跳转邮件、多账户管理 |
| 硬约束 | ① **无服务器** ② **无需保持网页常开** ③ **低资源消耗** ④ **自动运行完全静默** |

---

## 2. 约束边界（最高优先级，不可违反）

见仓库 `AGENTS.md`，核心两条铁律：

1. **规则 1**：自动运行（`chrome.alarms` 定时 / 全量检查 / 任何非用户点击触发路径）**绝对禁止打开新标签页，也禁止"常驻/保活标签"方案**。
   仅用户主动显式触发（Popup / Options 点击）才允许打开邮箱页同步会话。
2. **规则 3**：会话令牌（sid 等）优先用 `chrome.storage.local` 持久化；定时自动检查用「缓存 sid + Cookie 直调 API / API 模式回放」，仅在完全取不到数据时标记"需手动同步"。

> 这条约束直接决定了技术方案取舍：**凡需后台自动打开的方案一律排除**。

---

## 3. 技术方案决策

### 3.1 MV3 的物理边界

MV3 扩展无法直接建立 TCP（无 `chrome.sockets.tcp`、SW 会被终止），而国内邮箱（163/QQ/USTC）只有 IMAP/TCP 通道。因此后台读未读只有几条出口：

| 方案 | 需本地程序 | 需服务器 | 需常驻网页/标签 | 需逆向接口 | 无感度 | 稳定性 |
|------|:---:|:---:|:---:|:---:|:---:|:---:|
| **A. Native Messaging** | ✅ | ❌ | ❌ | ❌ | 中（安装期有感） | ★★★★★ |
| **B. SW 逆向 webmail HTTP 接口** | ❌ | ❌ | ❌ | ✅ | ★★★★★ | ★★（接口易变） |
| C. 内容脚本 + 保活邮箱标签 | ❌ | ❌ | ✅ | 部分 | ★★ | ★★★ |
| D. 服务器/Pages 代理 IMAP | ❌ | ✅ | ❌ | ❌ | ★★★ | ★★ + 隐私风险 |

### 3.2 取舍结论

- **Gmail**：官方 REST API + OAuth2（`chrome.identity`），纯插件、最稳，无争议。
- **国内邮箱（163/QQ/USTC）**：无标准开放接口。若坚持"纯插件 + 无服务器 + 不保活标签"，唯一路径是 **方案 B（SW 直调逆向 webmail 接口）**。
- **方案 D（Pages/边缘函数）已排除**：静态托管不能跑任意 TCP；即便 Serverless 也要把授权码托管到第三方云，违背"密码不出本地/无服务器"。
- **方案 C 已排除**：需常驻标签，违反 AGENTS 规则 1。
- **方案 A（Native Messaging）为兜底**：技术上最稳（标准 IMAP），但引入本地二进制、安装与分发复杂，用户最初要求避免。**保留为方案 B 验证失败时的回退选项。**

> **最终选型 = 方案 B 为主 + 内容脚本为辅的"混合检查模式"**，Gmail 走 REST 不受影响。

---

## 4. 当前整体架构：混合检查模式

```
SW 定时唤醒（alarm，每 N 分钟，全程静默、绝不自动开标签）
   │
   ├── ① SW 独立 API 直调（用持久化 sid + Cookie，credentials:'include'）
   │       ├── ✅ 读到未读数 → 更新 badge + 桌面通知
   │       └── ❌ 失败（sid 失效 / 无有效会话）
   │             ├── ② 检测已打开的邮箱标签 → 有则复用内容脚本读未读（不新开）
   │             └── ③ 无可用标签 → 标记「需手动同步」，不打扰用户
   │
   └── ④（学习机制）用户偶尔打开邮箱时，内容脚本提取 sid + 捕获真实 API 格式
          → 持久化（storage.local），供①在无页面时精确回放
```

关键实现文件（`extension/`，MV3）：

| 文件 | 职责 |
|------|------|
| `manifest.json` | MV3；权限 alarms/storage/notifications/tabs/cookies/scripting/declarativeNetRequest；内容脚本 `run_at: document_start` 全 frame 注入 |
| `background/service-worker.js` | 定时调度、混合检查编排、账户管理、消息路由 |
| `content/probe-content.js` | 提取 sid + in-origin 未读探测 + **API 请求捕获拦截器** |
| `providers/provider-163.js` | 163 SW 接口探测（js6/s RPC，带缓存 sid） |
| `providers/provider-qq.js` | QQ SW 接口探测（cgi-bin） |
| `shared/api-patterns.js` | API 模式捕获、归一化、持久化与回放 |
| `shared/debug.js` | 统一四级日志（Console + storage.session，串行化防丢失） |
| `shared/session-diagnose.js` | Cookie / 会话诊断 |
| `background/possibility-tests.js` | "全可能性"后台无标签测试（穷举 Cookie 附加策略） |
| `popup/` | Popup UI（状态 / 设置 / 统计 / 日志 / 调试），完整设置已收敛于 Popup |

> 详细设计、MV3 约束处理、存储分层等见《技术选型文档.md》。

---

## 5. 关键实验结论（截至当前，已实证）

> 以下为多轮真实登录环境测试得出的**事实**，是判断后续方向与可行性的依据。

### 5.1 Cookie 通道：结论已修正（✅ 全部打通）

早期曾判定"纯 SW 跨源 fetch 带不上会话 Cookie（根因 A）"。经 **"全可能性"测试**（穷举 A1–A5 / D1–D2 Cookie 附加策略）后**推翻该结论**：

- **163：SW 后台 fetch（`credentials:'include'` + 缓存 sid）Cookie 能穿透鉴权、到达 RPC 分发层**。
  证据：返回 `FR_INVALID_REQUEST: Invalid method getSessionInfo for module global`，属"已过鉴权才报方法名错误"；若 Cookie 没带上会直接回 `FA_SECURITY`/`FA_UNAUTHORIZED`（仅手动注入 Cookie 的 A4 策略触发）。
  → **163 后台纯 SW 读取已打通**（实测 unread=11）。

- **QQ：SW 后台 fetch 已验证成功**。
  API 端点：`GET https://wx.mail.qq.com/list/maillist?sid={sid}...`
  响应直接包含 `body.unread_num` 字段。
  → **QQ 后台纯 SW 读取已打通**（实测 unread=7）。

- **USTC：SW 后台 fetch 已验证成功**。
  API 端点：`GET http://mail.ustc.edu.cn/coremail/XT/jsp/mail.jsp?func=getAllFolders&sid={sid}`
  响应包含各文件夹的 `unreadMessageCount` 字段。
  → **USTC 后台纯 SW 读取已打通**（实测 unread=2）。

### 5.2 API 捕获链路：已降级为辅助功能

**当前状态**：API 捕获链路不是主要路径，已降级处理。

- 163/QQ/USTC 的 SW API 已手工抓包并硬编码到 `constants.js`
- 内容脚本的 API 拦截器主要用于**学习新接口格式**（当邮箱改版时）
- CSP 问题已修复（使用 `chrome.scripting` API 注入）

### 5.3 内容脚本 DOM 读取：稳定可靠（已验证）

| 邮箱 | 状态 | 未读数 | 方法 |
|------|------|--------|------|
| 163 | ✅ 成功 | 11 封 | SW API |
| QQ | ✅ 成功 | 7 封 | SW API |
| USTC | ✅ 成功 | 2 封 | SW API |
| Gmail | ✅ 已接入 | - | OAuth2 + REST API |

- 内容脚本可用于**用户已打开标签的复用场景**
- 受规则 1 限制，**不能作为自动后台主路径**
- Gmail 使用 OAuth2 授权，无需 sid

---

## 6. 当前进度（版本演进）
- Gmail 使用 OAuth2 授权，无需 sid

---

## 6. 当前进度（版本演进）

| 版本 | 阶段 | 状态 |
|------|------|:---:|
| v0.1 | 技术选型 / 方案 B PoC 骨架 | ✅ |
| v0.2.0 | 修复 sid 会话获取与使用 | ✅ |
| v0.3.0 | 内容脚本 in-origin 探测（方案 C 验证） | ✅ |
| v0.4.0/0.4.1 | 混合方案：内容脚本提 sid + SW 独立 API 检查；授权/获取清晰化 | ✅ |
| v0.5.0 | 修复无标签时 163/QQ 检查失败（Cookie 直调 + 会话恢复） | ✅ |
| v0.6.0 | 修复标签关闭后全量/自动检查失败 | ✅ |
| v0.7.0 | 后台自动检查不打开可见标签（sid 持久化 7 天） | ✅ |
| v0.8.0 | API 模式捕获与回放（含所有 frame / sendBeacon / EventSource） | ✅ 已实现，捕获待验证 |
| v0.9.0–0.9.2 | "全可能性"后台无标签测试（穷举 Cookie 策略）；日志串行化+fetch 超时；DNR/Cookie 注入杠杆探索 | ✅ |
| **v0.9.5** | **添加 Gmail REST API 支持 + 新邮件通知** | ✅ |
| **v1.0.0** | **首个商店发布版本（Chrome/Edge），统一版本、新增发布打包脚本** | ✅ **当前版本** |

> 工程说明：发布一键打包采用 `scripts/release.sh`（bash 封装 `zip`），替代原先的纯 Node 脚本，
> 支持 `pack` / `--check` / `--verify` / `--version`，产物在 `releases/`。

**已完成（能力）**
- MV3 混合检查调度、账户管理、sid 提取与持久化
- 163/QQ/USTC SW 独立探测（带完整日志、超时、Cookie 注入尝试）
- "全可能性" Cookie 附加策略穷举测试框架
- API 捕获拦截器（document_start、全 frame、含 sendBeacon/EventSource，CSP 绕过）
- Popup 多标签 UI（状态/探测/设置/统计/日志）、Options、debug/VALIDATION 指南
- **日志自动获取 API**（`window.getMailNotifierLogs`）
- **Gmail OAuth2 接入**（测试账号已配置）
- **新邮件通知系统**（检测未读数变化并发送桌面通知）
- **Gmail OAuth2 接入**（测试账号）
- **新邮件通知系统**（检测变化并发送桌面通知）

**结论性事实（影响后续）**
- ✅ **163 Cookie 通路已打通**，SW API 成功（unread=11）
- ✅ **QQ Cookie 通路已打通**，SW API 成功（unread=7）
- ✅ **USTC Cookie 通路已打通**，SW API 成功（unread=2）
- ✅ **Gmail OAuth2 已接入**，等待用户授权

---

## 7. 当前待办 / 下一步计划

按可行性优先级排序：

### Phase 1 — 生产化基础
- [x] **163 后台 API 打通**（已验证，unread=11）
- [x] **QQ 后台 API 打通**（已验证，unread=7）
- [x] **USTC 后台 API 打通**（已验证，unread=2）
- [ ] Gmail REST API 接入（OAuth2 + `gmail.readonly` scope）
- [ ] 接口失效自愈 / 热更规则
- [ ] 桌面通知、快速跳转、多账户管理 UI 完善
- [ ] 多邮箱统一通知

### Phase 2 — 高级功能
- [ ] API 模式捕获学习（当邮箱改版时自动学习新接口）
- [ ] 检查频率优化（智能退避、差异检查）
- [ ] 隐私增强（Cookie 清理、会话期限管理）

### 兜底方案
- [ ] 若 Gmail API 接入困难 → 考虑 Native Messaging（标准 IMAP）

> 注：以上 Checkbox 随每次 Issue 推进实时勾选更新，以本文件为当前权威进度。

---

## 8. 风险清单（更新）

| 风险 | 级别 | 状态/缓解 |
|------|:---:|------|
| 国内 webmail 真实接口未知 / 随改版变动 | 🟡 中 | 已硬编码当前接口；API 捕获学习机制待完善 |
| QQ/USTC 接口变动 | 🟡 中 | 需持续监控，支持热更规则 |
| 会话 sid 过期（无标签时） | 🟢 低 | storage.local 7 天 TTL + 自动恢复机制 |
| 风控 / 频控 | 🟢 低 | 控制检查频率（默认 5 分钟，可配置） |
| Cookie 在 SW 跨源请求附加 | ✅ 已实证 | 163/QQ/USTC 均已验证 |
| 内容脚本注入被 CSP 阻断 | ✅ 已修复 | 使用 chrome.scripting API 注入 |
| Gmail API 复杂性 | 🟡 中 | 需 OAuth2 流程，单独规划 |

---

## 9. 关联文档

- 《技术选型文档.md》 — 完整选型论证与逐版本演进记录
- `extension/debug/VALIDATION.md` — 真实环境验证步骤
- `extension/debug/README.md` — 调试使用指南
- `AGENTS.md` — 项目最高约束（规则 1 / 3）
