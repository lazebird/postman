# 跨平台统一换行符，避免不同 OS(Windows/macOS/Linux) 检出差异造成大面积 diff / lint 差异
* text=auto eol=lf

# 明确文本文件
*.js     text eol=lf
*.mjs    text eol=lf
*.json   text eol=lf
*.md     text eol=lf
*.sh     text eol=lf
*.yml    text eol=lf
*.yaml   text eol=lf
*.mjs    text eol=lf
*.css    text eol=lf
*.html   text eol=lf

# 二进制文件不转换
*.png    binary
*.jpg    binary
*.jpeg   binary
*.gif    binary
*.ico    binary
*.woff   binary
*.woff2  binary

# PNG 仅用于发布（图标位图），全仓库统一按文本处理会破坏二进制，这里明确排除站点图标
docs/mail-notifier/assets/icon.png binary

# GitHub Pages 关闭 Jekyll 处理的标记文件（必须保持 0 字节，不可被换行转换）
docs/.nojekyll -text
