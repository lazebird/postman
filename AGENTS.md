# AGENTS 规则与约束

> 本文件是面向 AI 编程助手（如 CodeBuddy 等）的**最高优先级规则文档**。
> 所有代码修改、功能开发、Bug 修复都必须遵守以下约束，**任何情况下不得违反**。

## 核心规则

### ⚠️ 规则 1：禁止自动打开新标签页，也禁止常驻标签

**背景**：历史上多次出现扩展在自动定时检查（alarm）或全量检查时自动打开新邮箱标签页的情况，
导致用户浏览器突然出现新标签、干扰当前操作、造成困惑。该行为已被用户明确、多次、严厉地否决。

**铁律（不可妥协）**：
- **自动运行时（alarm 定时检查 / 全量检查 / 任何非用户直接点击触发的路径）绝对禁止打开新标签页**，
  包括 `chrome.tabs.create()`、`chrome.tabs.update()` 导航到新 URL 等方式。
- **同样禁止「常驻标签」方案**：不得为了自动检查而把一个邮箱标签页常驻保活（包括固定/最小化到后台、长期不关闭以维持会话）。
  常驻标签会持续占用标签栏、造成资源与视觉干扰，用户体验同样很差，与自动开新标签同属不可接受的方案。
  凡是需要「一个一直开着的邮箱页面」才能工作的方案——包括保活后由内容脚本定时读未读——均须按此禁令处理，
  改为标记「需手动同步」引导用户，或用不依赖常驻页面的后台 API 直调 / API 模式回放方案。
- 禁止使用 `chrome.tabs.create({active: false})` 后台打开标签后自动关闭——仍然会造成视觉闪现、任务栏闪烁、标签栏跳动，均属不可接受。
- 仅**用户主动显式触发**（在 Popup / Options 页面中明确点击按钮）时，才允许打开邮箱标签页同步会话。

**不变量**：
1. `chrome.alarms` 触发的后台自动检查**绝不**创建或导航任何标签。
2. 检查模式（hybrid / content-script / sw-api）的任何分支都不得在非用户触发下创建标签。
3. 任何需要标签页能力的逻辑，在非用户触发下必须改为**降级方案**（如标记需手动同步、使用缓存数据），而不是静默创建标签。

**违反检测**：
- 代码 review 时重点检查 `chrome.tabs.create` / `chrome.tabs.update` 的调用路径，确认 `isUserInitiated(context)` 保护存在且逻辑正确。
- alarm 路径（`source === 'alarm'`）绝不允许触发任何 `tabs.create` / `tabs.update` 调用。
- 重点检查是否引入「常驻/保活标签」逻辑：任何为了自动检查而长期固定或维持一个邮箱标签的代码（如刻意不关闭标签、保活定时器维持会话、依赖常驻页面内容脚本轮询）均判定为违反本规则。

---

### 规则 2：用户体验优先于功能完整度

**用户体验是产品生命线，任何功能实现不得以牺牲用户体验为代价。**
- 后台运行必须**完全静默**（无标签闪现、无通知打扰、无视觉跳动）。
- 若某项功能无法在满足"静默后台运行"的前提下实现，宁可不实现或提示用户手动操作，也不得自动弹开标签。
- 在方案设计中优先考虑 API 直调、数据缓存等不依赖 UI 的技术路径。

---

### 规则 3：会话数据优先使用持久化存储

- sid 等会话令牌使用 `chrome.storage.local` 持久化，避免浏览器重启丢失。
- 用户主动授权（打开邮箱并登录）后，扩展应自动提取并缓存 sid，供后续纯后台 API 探测使用。
- 定时自动检查应优先使用缓存 sid + Cookie 直调 API；仅在完全无法获取数据时标记"需手动同步"，引导用户手动操作。
- **API 捕获模式**（页面打开时捕获的真实 API 请求格式）也应持久化保存，
  供后台无页面时精确复现。

---

### 规则 4：代码结构与抽象封装隔离要求

**代码结构必须保持清晰、解耦、可维护。任何新增代码或重构都必须遵守以下约定。**

#### 4.1 分层原则

代码按职责分为以下层次，各层之间只允许**上层依赖下层**，禁止反向依赖或跨层引用：

| 层级 | 目录 | 职责 | 可依赖 |
|------|------|------|--------|
| 入口层 | `background/` `popup/` `options/` | 事件路由、UI 逻辑 | 所有下层 |
| 领域层 | `providers/` | 各邮箱提供商的差异逻辑（响应解析、认证分析） | `shared/` |
| 基础设施层 | `shared/` | 通用工具、存储封装、数据逻辑、通信协议 | 仅依赖标准库 / chrome API |

- **background/service-worker.js 是事件路由入口，不是业务逻辑容器**。
  当业务逻辑超过 50 行时应抽到子模块，service-worker 只负责消息分发与结果回传。
- **providers/ 下每个文件只保留该提供商差异化的部分**。
  公共请求执行、sid 处理、响应解析辅助函数等不得在各 provider 中重复实现，
  必须收敛到 `shared/` 的公共模块。

#### 4.2 抽象封装要求

- **消除重复代码**：同一逻辑不得在三处及以上重复实现。发现重复时应抽取为共享模块。
  例如：多 provider 共用的「单端点请求探测」逻辑已封装在 `shared/endpoint-probe.js`，
  新增 provider 必须复用，不得重新实现。
- **面向接口编程**：模块通过**导出函数签名**对外暴露能力，不暴露内部实现细节。
  调用方只依赖模块的公共 API，不得直接访问其内部状态。
- **单一职责**：每个文件 / 模块只做一件事。避免在入口文件（service-worker、popup.js）
  中堆积大量与入口职责无关的辅助函数。
- **数据与逻辑分离**：常量定义收敛在 `shared/constants.js`；存储读写收敛在
  `shared/storage.js` / `shared/session-cache.js` / `shared/api-patterns.js`；
  请求执行收敛在 `shared/endpoint-probe.js` / `shared/session.js`。
  业务模块不得自行操作 `chrome.storage` 键名或重复实现数据映射。

#### 4.3 隔离要求

- **Provider 间隔离**：各邮箱 provider（163 / QQ / USTC / Gmail）的解析逻辑互相独立，
  不得互相引用。共通逻辑只允许通过 `shared/` 层复用。
- **UI 与逻辑隔离**：popup.js / options.js 只做 DOM 操作与消息收发，业务计算放在
  background 或 shared 层完成。UI 文件不得直接操作 `chrome.storage` 或调用 provider 函数。
- **存储层隔离**：所有 `chrome.storage` 读写必须经由 `shared/storage.js` 等封装模块，
  不直接在各业务文件中散落裸存储访问。
- **文件级隔离**：单文件超过 ~800 行时应评估拆分。文件内超长函数（>200 行）
  应抽取为独立函数或子模块。

#### 4.4 变更红线

- 重构必须**保持外部行为不变**（接口签名、返回结果结构、日志格式等不得随意变更）。
- 新增功能不得引入新的文件间循环依赖。
- 涉及架构调整的修改，必须更新本文档的「分层原则」表格以保持文档与现实一致。
- 提交 PR 时需在 PR 描述中说明本次变更是否符合以上隔离与封装要求。

---

### 规则 5：文件变更与提交

- 修改代码前先阅读本文档并确保遵守上述约束。
- 涉及标签操作或自动检查逻辑的修改，必须在提交说明中明确说明是否违反规则 1。
- 新增功能不得破坏现有约束。
- 静态审查：所有 JS 代码必须通过 `scripts/lint.sh`（Prettier + ESLint）校验后才能提交。

---

## 适用场景速查

| 场景 | 是否允许开标签 | 说明 |
|------|---------------|------|
| alarm 定时自动检查 | ❌ 禁止 | 应使用缓存 sid + SW API 直调 + API 模式回放 |
| 全量检查（用户手动点击） | ⚠️ 谨慎 | 仅在确实需要且用户已明确授权时 |
| Popup/Options 中点击"同步会话" | ✅ 允许 | 用户主动操作 |
| Popup/Options 中点击"打开邮箱" | ✅ 允许 | 用户主动操作 |
| 定时检查发现 sid 失效 | ❌ 禁止 | 标记"需手动同步"，引导用户 |
| 定时检查发现已有打开邮箱标签 | ✅ 可复用 | 直接读用户已打开的标签，不新建、不保活；标签关闭即视为无可用标签 |
| 为自动检查而「常驻/保活一个邮箱标签」 | ❌ 禁止 | 禁止固定/最小化长期保活页面以维持会话或供内容脚本轮询 |
| 定时检查发现 sid 失效且无已开标签 | ❌ 禁止 | 不允许自动开标签或自动常驻标签去恢复，标记"需手动同步"引导用户 |

---

## API 捕获与回放策略

### 如何实现「不开标签也能读未读」？

核心链路：
1. 用户偶尔打开邮箱（登录态）→ 内容脚本捕获页面实际发出的 API 请求格式
   （URL、method、headers、body），sid 自动替换为 `{sid}` 占位符
2. 捕获的 API 模式持久化到 `chrome.storage.local`（`api_patterns_163` / `api_patterns_qq`）
3. 用户关闭邮箱标签后 → SW 后台自动检查时，直接使用**捕获的真实 API 请求格式**进行调用
4. 浏览器 Cookie 自动附带（`credentials: 'include'`），sid 从缓存读取并替换

此策略避免了「猜测接口格式」的不确定性，利用页面打开时的真实 API 调用来
「学习」正确格式，实现后台无感检查。
